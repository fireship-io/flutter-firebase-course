name: api_client
description: Firebase quizzes API client.
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  cloud_firestore: ^4.8.0
  firebase_auth: ^4.6.2
  firebase_core: ^2.13.1
  flutter:
    sdk: flutter
  google_sign_in: ^6.1.3
  sign_in_with_apple: ^4.3.0

dev_dependencies:
  very_good_analysis: ^5.0.0+1


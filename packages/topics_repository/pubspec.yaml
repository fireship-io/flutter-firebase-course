name: topics_repository
description: Topics repository.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=2.16.0 <3.0.0'

dependencies:
  data_providers:
    path: ../data_providers
  flutter:
    sdk: flutter
  shared:
    path: ../shared

dev_dependencies:
  build_runner: ^2.1.8
  json_serializable: ^6.1.5
  very_good_analysis: ^2.4.0

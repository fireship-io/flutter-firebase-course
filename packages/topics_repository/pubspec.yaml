name: topics_repository
description: Topics repository.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=2.17.0 <3.0.0'

dependencies:
  data_providers:
    path: ../data_providers
  flutter:
    sdk: flutter
  shared:
    path: ../shared

dev_dependencies:
  build_runner: ^2.1.10
  json_serializable: ^6.2.0
  very_good_analysis: ^2.4.0

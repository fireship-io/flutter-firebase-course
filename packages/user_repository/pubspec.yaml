name: user_repository
description: User repository.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  data_sources:
    path: ../data_sources

dev_dependencies:
  build_runner: ^2.0.5
  json_serializable: ^4.1.3
  very_good_analysis: ^2.1.2
